import Loader from './../components/Loader'

let MyMixins = {
    data() {
        return {
            is_loading: true,
        }
    },
    components: {
        Loader,
    },
};

export default MyMixins;
